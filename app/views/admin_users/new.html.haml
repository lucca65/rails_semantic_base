.ui.page.grid
  .sixteen.wide.column
    %h1= t('.header')
    - if @admin_user.errors.any?
      .ui.info.icon.message.notice
        %i.close.icon
        %i.info.icon
        .content
          .header
            Info
          %p
            = pluralize(@admin_user.errors.count, t('.error'))
            = t('.error_message')
            %ul
              - @admin_user.errors.full_messages.each do |msg|
                #li= msg
    = render 'form'